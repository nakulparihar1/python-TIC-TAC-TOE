# python-TIC-TAC-TOE
This Python project implements a simple Tic-Tac-Toe (X/O) game using the Pygame library. It displays a 3×3 grid where two players take turns by clicking on cells. The game alternates between X and O, updating the board visually in real time until the window is closed.
